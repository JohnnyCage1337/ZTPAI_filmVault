version: "3.8"

services:
  web:
    build:
      context: ./backend
    env_file:
      - .env
    volumes:
      - ./backend:/app
    ports:
      - "8000:8000"
    environment:
      - CORS_ALLOW_ALL_ORIGINS=True
